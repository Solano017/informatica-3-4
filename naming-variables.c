#include <stdio.h>

int main(void)
{

int number; // We remove ! because we can´t have any special characters
int test_value; // We change the space for a _ since spaces are not allowed
int example1; // Variables always have to start with letters, cant start with a number
float int_float; // int by itself was a reserved keyword so i changed it into something valid.
printf("Variables created successfully.\n");

}
